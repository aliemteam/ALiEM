version: '3'
services:
    wordpress:
        image: visiblevc/wordpress:0.15.2-php7.1
        ports:
            - 8080:80
        volumes:
            # Database
            - ./data:/data
            # Plugins
            - ./wp-content/plugins/fanciest-author-box:/app/wp-content/plugins/fanciest-author-box
            - ./wp-content/plugins/fusion-builder:/app/wp-content/plugins/fusion-builder
            - ./wp-content/plugins/fusion-core:/app/wp-content/plugins/fusion-core
            # Themes
            - ./dist/aliem:/app/wp-content/themes/aliem
            - ./wp-content/themes/Avada:/app/wp-content/themes/Avada
            # Uploads
            - ./wp-content/uploads:/app/wp-content/uploads
        environment:
            DB_NAME: wordpress
            DB_PASS: root
            DB_USER: root
            PLUGINS: >-
                academic-bloggers-toolkit,
                [abt-deprecated-peer-review]https://github.com/dsifford/abt-deprecated-peer-review/archive/master.zip,
                broken-link-checker,
                co-authors-plus,
                disqus-comment-system,
                wysija-newsletters,
                mce-table-buttons,
                shortcodes-ultimate,
                w3-total-cache,
                wp-smushit,
                wordpress-seo,
                query-monitor,

                header-footer-code-manager,
                riveted,
                wp-hide-post,
            WP_DEBUG_DISPLAY: 'true'
            WP_DEBUG_LOG: 'true'
            WP_DEBUG: 'true'
            SEARCH_REPLACE: https://www.aliem.com,http://localhost:8080
    db:
        image: mariadb:10
        environment:
            MYSQL_ROOT_PASSWORD: root
        volumes:
            - data:/var/lib/mysql
    phpmyadmin:
        image: phpmyadmin/phpmyadmin
        environment:
            MYSQL_ROOT_PASSWORD: root
        ports:
            - 22222:80
volumes:
    data:
